/**
 * Validates:
 * - Localization files being present and valid json
 */
import { Validator } from './validator';
import { ComponentSet, GetFileContentType } from '../models';
export declare class LocalizationValidator extends Validator {
    private filePaths;
    protected getFileContent: GetFileContentType;
    constructor(error: (errorMessage: string) => false, definition: ComponentSet, filePaths: Set<string>, getFileContent: GetFileContentType);
    validate(): Promise<void>;
    validateLocalizationFile(localizationFile: string): Promise<void>;
    getLocalizationFiles(): string[];
}
