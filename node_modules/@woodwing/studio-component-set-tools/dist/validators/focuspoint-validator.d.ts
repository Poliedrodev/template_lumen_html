/**
 * Validates usage of focuspoint of image-editor property
 */
import { Validator } from './validator';
export declare class FocuspointValidator extends Validator {
    validate(): Promise<void>;
}
