/**
 * Validates usage of fitting control type
 * - only one per a component
 */
import { Validator } from './validator';
export declare class FittingValidator extends Validator {
    private countPerComponent;
    validate(): Promise<void>;
}
