/**
 * Validates components
 * - if names are unique
 * - if icons are present
 * - if templates are present
 * - if styles are present
 * - also if generic styles are present: design.scss, design.css, _common.scss
 */
import { Validator } from './validator';
import { ComponentSet } from '../models';
export declare class ComponentsValidator extends Validator {
    private filePaths;
    constructor(error: (errorMessage: string) => false, definition: ComponentSet, filePaths: Set<string>);
    validate(): Promise<void>;
}
