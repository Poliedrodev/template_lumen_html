/**
 * Validates if conversion shortcuts components are exist
 */
import { Validator } from './validator';
export declare class ConversionShortcutsValidator extends Validator {
    validate(): Promise<void>;
}
