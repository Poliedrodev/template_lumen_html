/**
 * Validates:
 * - Whether icons have the correct format
 */
import { Validator } from './validator';
import { ComponentSet, GetFileContentType } from '../models';
export declare class IconsValidator extends Validator {
    protected getFileContent: GetFileContentType;
    private supportedFormats;
    constructor(error: (errorMessage: string) => false, definition: ComponentSet, getFileContent: GetFileContentType);
    validate(): Promise<void>;
}
