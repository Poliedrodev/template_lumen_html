/**
 * Validates component package size:
 * - Maximum number of files in the component set.
 * - Maximum total file size of the component set.
 * - Maximum number of files in the custom data folder.
 * - Maximum total file size of the custom data folder.
 */
import { ComponentSet, GetFileSize } from '../models';
import { Validator } from './validator';
export declare class PackageValidator extends Validator {
    private filePaths;
    private getFileSize;
    constructor(error: (errorMessage: string) => false, definition: ComponentSet, filePaths: Set<string>, getFileSize: GetFileSize);
    validate(): Promise<void>;
    private validateComponentSetSize;
    private validateComponentSetFileCount;
    private validateCustomData;
    private validateCustomDataFileCount;
    private validateCustomDataSize;
    private calculateFolderSize;
}
export declare function validateTotalSize(size: number): string | void;
