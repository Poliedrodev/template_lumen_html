/**
 * Validates if conversion rules reference to existing components and directives
 */
import { Validator } from './validator';
export declare class ConversionRulesValidator extends Validator {
    validate(): Promise<void>;
}
