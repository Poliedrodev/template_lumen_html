/**
 * Validates properties
 * - if names are unique
 * - if icons are present
 * - if reserved words are not used as names
 * - if doc-media properties have a media-properties control type
 */
import { Validator } from './validator';
import { ComponentSet } from '../models';
export declare class PropertiesValidator extends Validator {
    protected filePaths: Set<string>;
    constructor(error: (errorMessage: string) => false, definition: ComponentSet, filePaths: Set<string>);
    validate(): Promise<void>;
    private validateComponent;
    private validateProperty;
    private validatePropertyName;
    private validateReservedPropertyName;
    private validateNamelessProperty;
    private validateRadioPropertyIcons;
    private validateCheckBoxValue;
    private validateChildProperties;
}
