/**
 * Validates restrictChildren properties option
 */
import { Validator } from './validator';
export declare class RestrictChildrenValidator extends Validator {
    private hasSlideshowDirective;
    validate(): Promise<void>;
}
