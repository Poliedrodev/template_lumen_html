import { ComponentSet } from '../models';
import { Validator } from './validator';
export declare class CustomStylesValidator extends Validator {
    private filePaths;
    constructor(error: (errorMessage: string) => false, definition: ComponentSet, filePaths: Set<string>);
    validate(): Promise<void>;
    private checkCustomStylesFolder;
    private validateDefaultCustomStyleExists;
}
